---
description: Show today's calendar events
allowed-tools: Bash
---

Run `idag` to show today's calendar events.

If `idag` is not installed, tell the user to run:
```
brew install hamedmp/tap/idag
```

Present the events in a clean, readable format.
